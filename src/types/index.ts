export type typesForBestGearCatergories = {
    id: number,
    title: string,
    imageUrl: string
}

export type imageProps = {
    title: string;
    imageUrl: string;
}

export type typesForLatestPromotions = {
    id: number;
    imageUrl: string;
}